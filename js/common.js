// common.js// – Sticky header hides on scroll-down, re-shows on scroll-up// – particles.js init if #particles-js existsdocument.addEventListener('DOMContentLoaded', () => {  const header = document.querySelector('header');  if (header) {    let lastY = window.pageYOffset;    let ticking = false;    function updateHeader() {      const currY = window.pageYOffset;      if (currY > lastY && currY > header.offsetHeight) {        header.classList.add('header-hidden');      } else if (currY < lastY || currY <= header.offsetHeight) {        header.classList.remove('header-hidden');      }      lastY = currY;      ticking = false;    }    window.addEventListener('scroll', () => {      if (!ticking) {        window.requestAnimationFrame(updateHeader);        ticking = true;      }    });    updateHeader();  }  // particles.js loader  if (window.particlesJS && document.getElementById('particles-js')) {    particlesJS.load('particles-js', 'particles.json', () => {      console.log('particles.js loaded');    });  }});