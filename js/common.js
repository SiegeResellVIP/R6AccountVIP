// common.js// – Sticky header: hides on scroll-down, re-shows on any scroll-up// – Particles.js init if #particles-js existsdocument.addEventListener('DOMContentLoaded', () => {  const header = document.querySelector('header');  if (header) {    let lastScrollY = window.pageYOffset;    let ticking    = false;    function updateHeader() {      const currentY = window.pageYOffset;      if (currentY > lastScrollY && currentY > header.offsetHeight) {        header.classList.add('header-hidden');      } else if (currentY < lastScrollY || currentY <= header.offsetHeight) {        header.classList.remove('header-hidden');      }      lastScrollY = currentY;      ticking    = false;    }    window.addEventListener('scroll', () => {      if (!ticking) {        window.requestAnimationFrame(updateHeader);        ticking = true;      }    });    // ensure correct state on load    updateHeader();  }  // particles.js loader  if (window.particlesJS && document.getElementById('particles-js')) {    particlesJS.load('particles-js', 'particles.json', () => {      console.log('particles.js loaded');    });  }});